import { FiltrocedulaPipe } from './filtrocedula.pipe';

describe('FiltrocedulaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrocedulaPipe();
    expect(pipe).toBeTruthy();
  });
});
